import type { ComponentPropsWithoutRef } from 'react';

export type {{ComponentName}}Variant = 'default' | 'primary' | 'secondary';
export type {{ComponentName}}Size = 'sm' | 'md' | 'lg';

// For Ark-based primitives, replace this DOM fallback with target Ark Root/part props.
export interface {{ComponentName}}Props extends ComponentPropsWithoutRef<'div'> {
  /**
   * Visual variant
   * @default 'default'
   */
  variant?: {{ComponentName}}Variant;

  /**
   * Size variant
   * @default 'md'
   */
  size?: {{ComponentName}}Size;
}
