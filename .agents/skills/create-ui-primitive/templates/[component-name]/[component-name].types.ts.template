import type { ComponentPropsWithoutRef } from 'react';

export type {{ComponentName}}Variant = 'solid' | 'soft' | 'outline' | 'ghost';
export type {{ComponentName}}Size = 'sm' | 'md' | 'lg';
export type {{ComponentName}}Tone = 'neutral' | 'accent' | 'danger';

// For Ark-based primitives, replace this DOM fallback with the target Ark Root props.
// Each exported Ark slot should have its own props type derived from the matching Ark part.
export interface {{ComponentName}}RootProps extends ComponentPropsWithoutRef<'div'> {
  /**
   * Visual variant
   * @default 'soft'
   */
  variant?: {{ComponentName}}Variant;

  /**
   * Size variant
   * @default 'md'
   */
  size?: {{ComponentName}}Size;

  /**
   * Color tone
   * @default 'neutral'
   */
  tone?: {{ComponentName}}Tone;
}
